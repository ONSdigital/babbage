{{#minify}}
    {{!-- This code has to be minified or else the spacing breaks adding the contentTitle to the GTM data layer --}}
    {{#if_any (eq type "timeseries_dataset") (eq type "dataset")}} {{#resolve (parentPath uri)}}{{description.title}}{{/resolve}}
    {{else if description.title}} {{description.title}}
    {{else if title}}{{{sub (sup title clear=true) clear=true}}}
    {{else}}Page not found
    {{/if_any}}
{{/minify}}