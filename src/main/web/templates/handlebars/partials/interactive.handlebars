{{#if additionalUrl}}<div class="pym-interactive--display-inline-flex">{{/if}}
<div class="js--show">

    <div class="pym-interactive{{#if_eq full-width "true"}}{{#if additionalUrl}} pym-interactive-half-width--full{{else}} pym-interactive--full-width{{/if}}{{else}}{{#if additionalUrl}} pym-interactive-half-width{{/if}}{{/if_eq}}" id="{{id}}" data-url="{{url}}"></div>

    <details class="details">
        <summary class="details__summary">Embed code</summary>
        <div class="details__body">
            <label class="embed-code__label" for="embed-{{id}}">Embed this interactive<br/></label>
            <input class="embed-code__code {{#if additionalUrl}}width-md--15{{else}}width-md--32{{/if}}"
        value='<iframe width="100%" src="{{#if (containsAny url "http" "https")}}{{url}}{{else}}{{#if is_dev}}http://{{location.host}}{{else}}https://{{location.hostname}}{{/if}}{{url}}{{/if}}"></iframe>'
                   id="embed-{{id}}"
                   name="embed-{{id}}"
                   readonly
            />
            <span class="embed-code__success-container">
                <button id="" data-id="{{id}}" type="button" class="btn btn--primary embed-code__button js-embed-code-copy">Copy</button>
                <span role="status"></span>
            </span>
        </div>
    </details>

</div>
{{#if additionalUrl}}
<div class="js--show">

    <div class="pym-interactive{{#if_eq full-width "true"}} pym-interactive-half-width--full{{else}} pym-interactive-half-width{{/if_eq}}" id="{{additionalId}}" data-url="{{additionalUrl}}"></div>

    <details class="details">
        <summary class="details__summary">Embed code</summary>
        <div class="details__body">
            <label class="embed-code__label" for="embed-{{additionalId}}">Embed this interactive<br/></label>
            <input class="embed-code__code width-md--15"
        value='<iframe width="100%" src="{{#if (containsAny additionalUrl "http" "https")}}{{additionalUrl}}{{else}}{{#if is_dev}}http://{{location.host}}{{else}}https://{{location.hostname}}{{/if}}{{additionalUrl}}{{/if}}"></iframe>'
                   id="embed-{{additionalId}}"
                   name="embed-{{additionalId}}"
                   readonly
            />
            <span class="embed-code__success-container">
                <button id="" data-id="{{additionalId}}" type="button" class="btn btn--primary embed-code__button js-embed-code-copy">Copy</button>
                <span role="status"></span>
            </span>
        </div>
    </details>

</div>
</div>
{{/if}}